import Foundation

enum Constants {
    static let publishableKey = "UBLISHABLE_KEY_HERE"
  static let baseURLString = "http://localhost:9000"
  static let defaultCurrency = "usd"
  static let defaultDescription = "Purchase from Flowerish app"
}
